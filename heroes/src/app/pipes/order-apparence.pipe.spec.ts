import { OrderApparencePipe } from './order-apparence.pipe';

describe('OrderApparencePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderApparencePipe();
    expect(pipe).toBeTruthy();
  });
});
