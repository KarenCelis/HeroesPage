import { SearchApparencePipe } from './search-apparence.pipe';

describe('SearchApparencePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchApparencePipe();
    expect(pipe).toBeTruthy();
  });
});
